services:
  ai-factory:
    build: .
    volumes:
      # This maps your local folder 'output_projects' to the container's '/projects'
      - ./output_projects:/projects
    env_file:
      - .env
    stdin_open: true
    tty: true
