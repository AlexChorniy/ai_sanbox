services:
  ai-factory:
    build: .
    container_name: ai_factory_container
    volumes:
      - .:/app
      - ./projects:/projects # Your AI-generated code will appear here
    env_file:
      - .env
    stdin_open: true
    tty: true
    restart: "no"
